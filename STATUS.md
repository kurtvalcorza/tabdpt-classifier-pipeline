# Release status

Current status: **Candidate — standalone tutorial source complete, awaiting clean-runtime execution**. The pipeline package (three modules, all carried), the offline unit suite, `MODEL_CARD.md`, and the standalone tutorial pair — `tutorials/tabdpt_classifier_colab.ipynb` (`E2E`) and `tutorials/tabdpt_classifier_artifact_inference_colab.ipynb` (`ARTIFACT-INFERENCE`), both generated by `tools/build_notebook.py` under DIMER Notebook Specification 1.1 §3.6 — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks for both notebooks). The previous repository-installing pair (spec 1.0) has a recorded local clean-kernel run dated 2026-09-11 (see `tutorials/README.md` and `docs/release-verification.md`); that record does not transfer to the standalone carrier. The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit of the standalone notebooks is recorded in `docs/release-verification.md`.
